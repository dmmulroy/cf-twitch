{
  "id": "1d38f1ae",
  "title": "TDD migrate KeyboardRaffleDO to Agent class",
  "tags": [
    "refactor",
    "tdd",
    "durable-objects",
    "keyboard-raffle"
  ],
  "status": "done",
  "created_at": "2026-04-08T12:55:45.848Z"
}

Completed.

Implemented:
- Migrated `apps/api/src/durable-objects/keyboard-raffle-do.ts` from `DurableObject<Env>` to `Agent<Env, KeyboardRaffleAgentState>`.
- Moved bootstrap migration work into `onStart()` wrapped with `ctx.blockConcurrencyWhile(...)`.
- Kept SQLite/Drizzle as the sole source of truth for raffle history and leaderboard queries.
- Removed unused no-op lifecycle methods `onStreamOnline()` / `onStreamOffline()` and dropped the unused `StreamLifecycleHandler` implementation.
- Preserved the meaningful public RPC surface: `recordRoll`, `deleteRollById`, `getLeaderboard`, `getUserStats`, `getUserStatsByDisplayName`, `getClosestRecord`.

Tests:
- Rewrote `apps/api/src/__tests__/durable-objects/keyboard-raffle-do.test.ts` to exercise behavior through the public Agent/stub interface instead of `runInDurableObject(...)`.
- Added explicit Agent stub naming in setup (`setName(...)`) and used `getStub("KEYBOARD_RAFFLE_DO", raffleName)` for wrapped public calls.
- Fixed outdated assertions for `recordRoll()` to assert the actual return shape `{ roll, isNewRecord }`.
- Added a public-behavior test for `getClosestRecord()` and for stricter `isNewRecord` behavior.
- Removed obsolete lifecycle no-op tests.

Validation:
- ✅ `cd apps/api && pnpm test src/__tests__/durable-objects/keyboard-raffle-do.test.ts`
- ✅ `cd apps/api && pnpm typecheck`
- ⚠️ `cd apps/api && pnpm test src/__tests__/durable-objects/keyboard-raffle-saga-do.test.ts` failed with pending fetch interceptors / AchievementsDO `event_history` setup issue, which appears unrelated to the KeyboardRaffleDO migration and was not changed in this task.
