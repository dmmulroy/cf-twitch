{
  "id": "e2090fd5",
  "title": "Migrate AchievementsDO to Agent with RGR TDD",
  "tags": [
    "durable-objects",
    "agents",
    "tdd",
    "achievements"
  ],
  "status": "closed",
  "created_at": "2026-04-07T14:10:47.085Z"
}

Migrated `apps/api/src/durable-objects/achievements-do.ts` from `DurableObject<Env>` to `Agent<Env, AchievementsAgentState>`.

Completed with behavior-first TDD via new integration tests in `apps/api/src/__tests__/durable-objects/achievements-do.test.ts` covering:
- first request of stream unlocks `first_request` + `stream_opener`
- `onStreamOnline()` resets session achievements while preserving cumulative progress
- raffle roll outcomes unlock `first_roll`, `close_call`, and `closest_ever`

Implementation details:
- added `AchievementsAgentState`
- moved DB migration bootstrap into `onStart()` with `ctx.blockConcurrencyWhile(...)`
- preserved public RPC surface
- used small Agent state for current stream coordination (`isStreamLive`, `currentStreamStartedAt`)
- updated tests to set explicit Agent stub names per repo migration guidance

Validation:
- `cd apps/api && pnpm exec vitest run src/__tests__/durable-objects/achievements-do.test.ts`
- `cd apps/api && pnpm run typecheck`
