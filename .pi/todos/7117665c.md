{
  "id": "7117665c",
  "title": "Address song queue review feedback",
  "tags": [
    "code-review",
    "song-queue"
  ],
  "status": "done",
  "created_at": "2026-04-13T14:25:51.080Z"
}

Addressed review feedback in:

- `apps/api/src/durable-objects/song-queue-do.ts`
  - `connectRpc()` no longer accepts a name and now uses the singleton constant internally.
  - exported `SONG_QUEUE_DO_NAME` for shared singleton lookup.
  - internal shared methods are now `private`.
  - kept shared private implementations with a clarifying comment because `@rpc` serializes `Result`s while the `RpcTarget` handle needs raw `Result`s.
- `apps/api/src/lib/song-queue-client.ts`
  - removed manual `ensureSongQueueName()` bootstrap helper.
  - `getSongQueue()` no longer accepts a name and just connects to the singleton DO.
  - `connectRpc()` is called without arguments.
- `apps/api/src/lib/rpc-result.ts`
  - added thorough JSDoc for transport helpers including params, return values, throws behavior, and examples.

Validation:
- `pnpm typecheck`
- `cd apps/api && pnpm exec vitest run src/__tests__/durable-objects/get-stub-agent-name.test.ts src/__tests__/durable-objects/song-queue-do.test.ts`
