import { defineConfig } from "vite-plus";

/** Inherits strict root validation for the portable contracts source package. */
const contractsViteConfig = defineConfig({});

export default contractsViteConfig;
